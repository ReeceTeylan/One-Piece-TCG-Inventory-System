# Base URL of the backend API. In dev, leave as /api to use the Vite proxy.
VITE_API_URL=/api

# Optional: absolute base URL for uploaded images if served from a different host than the API.
# Leave empty to use the same origin (dev proxy / same host in prod).
VITE_ASSET_URL=
